"""EMTPSolver — placeholder that re-exports from the legacy module.

Once Round 5 is complete this file will contain the actual EMTPSolver
class and the legacy module will become a thin compatibility shim.
"""

from emtp_solver_v3 import EMTPSolver  # noqa: F401
